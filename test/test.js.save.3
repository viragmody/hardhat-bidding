// Tests for bidding.sol

const { expect } = require('chai');
const { waffle } = require('hardhat');
const { provider } = waffle.provider;

const [owner, addr1, addr2, addr3] = await ethers.getSigners();

describe("Bidding", function () {
    it("Contract Deployed", async function() {
        const [owner, addr1, addr2, addr3] = await ethers.getSigners();        const Bidding = await ethers.getContractFactory("Bidding");
        const hardhatBidding = await Bidding.deploy(50, 50, "cat");
//        expect(await hardhatBidding.owner()).to.equal(owner.address);
//        expect (await hardhatBidding.test()).to.equal(0);
//        await hardhatBidding.connect(addr1).depositBid(10);
//        await hardhatBidding.connect(addr2).depositBid(20);
//        await hardhatBidding.connect(addr3).depositBid(30);
//        expect (await hardhatBidding.test()).to.equal(addr3.address);
    });
});
